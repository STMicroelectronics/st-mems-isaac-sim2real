# ============================================================
# st-mems-isaac-sim2real — Python Requirements
# ============================================================
# Use an isolated Python environment for local plotting/validation.
# Isaac Sim 5.1.0 uses Python 3.11; Isaac Sim 6.0.0 uses Python 3.12.
# Install with: pip install -r requirements.txt
#
# NOTE: omni.* packages (omni.usd, omni.physx, omni.timeline, etc.)
# are bundled with Isaac Sim and do NOT need to be installed here.
# These requirements cover only the verification and plotting scripts.
# ============================================================

# --- Verification Script (verification_script.py) ---
numpy>=1.24.0

# --- Plotting Script (plot_verification.py) ---
pandas>=1.5.0
matplotlib>=3.6.0
